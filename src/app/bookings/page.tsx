import { AppShell } from "@/components/layout/AppShell";import { bookings } from "@/lib/mockData";import { aed } from "@/lib/utils";import { Button } from "@/components/ui/Button";export default function Bookings(){return <AppShell><h1 className="mb-4 text-3xl font-black">حجوزاتي</h1>{['نشط','بانتظار الموافقة','سابق'].map(status=><section key={status} className="mb-5"><h2 className="mb-2 text-xl font-black">{status==='نشط'?'حجوزات نشطة':status==='سابق'?'حجوزات سابقة':'حجوزات بانتظار الموافقة'}</h2><div className="space-y-3">{bookings.filter(b=>b.status===status).map(b=><div key={b.id} className="rounded-[1.5rem] bg-white p-4 shadow-sm"><div className="flex justify-between gap-3"><b>{b.space}</b><span className="text-blue-700 font-black">{aed(b.price)}</span></div><p className="text-sm text-slate-500">{b.date} — {b.status}</p><div className="mt-3 grid grid-cols-2 gap-2"><Button href={`/listings/${b.listingId}`} variant="ghost">التفاصيل</Button><Button href="/messages/m1" variant="secondary">مراسلة المضيف</Button></div></div>)}</div></section>)}</AppShell>}
