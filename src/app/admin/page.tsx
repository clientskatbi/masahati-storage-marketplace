import { AppShell } from "@/components/layout/AppShell";import { adminStats, listings } from "@/lib/mockData";import { aed } from "@/lib/utils";export default function Admin(){return <AppShell><h1 className="mb-4 text-3xl font-black">Admin تجريبي</h1><div className="grid grid-cols-2 gap-3">{[['عدد المستخدمين',adminStats.users],['عدد المساحات',adminStats.spaces],['عدد الحجوزات',adminStats.bookings],['إجمالي GMV',aed(adminStats.gmv)]].map(([k,v])=><div key={k} className="rounded-[1.5rem] bg-white p-4 shadow-sm"><small className="text-slate-500">{k}</small><p className="text-2xl font-black text-blue-700">{v}</p></div>)}</div><h2 className="mt-8 mb-3 text-2xl font-black">المساحات المضافة</h2><div className="space-y-3">{listings.map(l=><div key={l.id} className="rounded-[1.5rem] bg-white p-4 shadow-sm"><b>{l.title}</b><p className="text-sm text-slate-500">{l.city} — {l.type}</p><div className="mt-3 grid grid-cols-3 gap-2"><button className="rounded-xl bg-emerald-50 p-2 font-bold text-emerald-700">قبول</button><button className="rounded-xl bg-red-50 p-2 font-bold text-red-700">رفض</button><button className="rounded-xl bg-blue-50 p-2 font-bold text-blue-700">تمييز كموثّق</button></div></div>)}</div></AppShell>}
