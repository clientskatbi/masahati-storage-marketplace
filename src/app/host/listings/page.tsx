import { AppShell } from "@/components/layout/AppShell";import { listings } from "@/lib/mockData";import { ListingCard } from "@/components/listings/ListingCard";export default function HostListings(){return <AppShell><h1 className="mb-4 text-3xl font-black">مساحات المضيف</h1><div className="grid gap-4 md:grid-cols-2">{listings.slice(0,6).map(l=><ListingCard key={l.id} listing={l}/>)}</div></AppShell>}
