import { AppShell } from "@/components/layout/AppShell";import { getListing } from "@/lib/mockData";import { BookingForm } from "@/components/booking/BookingForm";export default async function BookingPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const l=getListing(id);return <AppShell><h1 className="mb-4 text-3xl font-black">حجز {l.title}</h1><BookingForm listing={l}/></AppShell>}
