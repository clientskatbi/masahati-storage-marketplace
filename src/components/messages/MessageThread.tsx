"use client";import { useState } from "react";import { conversations } from "@/lib/mockData";
export function MessageThread({id}:{id:string}){const c=conversations.find(x=>x.id===id)||conversations[0];const [msgs,setMsgs]=useState(c.messages);const [text,setText]=useState('');function send(){if(!text.trim())return;setMsgs([...msgs,text]);setText('');window.dispatchEvent(new CustomEvent('toast',{detail:'تم إرسال الرسالة'}))}return <div className="space-y-4"><h1 className="text-2xl font-black">محادثة مع {c.name}</h1><div className="min-h-[55vh] rounded-[1.8rem] bg-white p-4 shadow-sm">{msgs.map((m,i)=><p key={i} className={`mb-3 max-w-[82%] rounded-2xl p-3 text-sm ${i%2?'mr-auto bg-blue-700 text-white':'bg-slate-100 text-slate-800'}`}>{m}</p>)}</div><div className="fixed inset-x-0 bottom-20 mx-auto flex max-w-5xl gap-2 bg-slate-50 p-4"><input value={text} onChange={e=>setText(e.target.value)} className="flex-1 rounded-2xl border p-4" placeholder="اكتب رسالة"/><button onClick={send} className="rounded-2xl bg-blue-700 px-5 font-bold text-white">إرسال</button></div></div>}
