"use client";import { useEffect,useState } from "react";export function ToastHost(){const [msg,setMsg]=useState('');useEffect(()=>{const h=(e:Event)=>{setMsg((e as CustomEvent).detail);setTimeout(()=>setMsg(''),2200)};window.addEventListener('toast',h);return()=>window.removeEventListener('toast',h)},[]);return msg?<div className="fixed left-4 right-4 top-4 z-[70] mx-auto max-w-sm rounded-2xl bg-slate-900 p-4 text-center font-bold text-white shadow-xl">{msg}</div>:null}
