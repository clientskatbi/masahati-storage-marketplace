"use client";import Link from "next/link";import { usePathname } from "next/navigation";import { Home,Search,CalendarCheck,MessagesSquare,UserRound } from "lucide-react";
const items=[['/','الرئيسية',Home],['/search','البحث',Search],['/bookings','حجوزاتي',CalendarCheck],['/messages','الرسائل',MessagesSquare],['/profile','حسابي',UserRound]] as const;
export function BottomNav(){const p=usePathname();return <nav className="fixed inset-x-0 bottom-0 z-50 mx-auto max-w-5xl border-t border-border bg-card/95 px-2 pb-2 pt-1 shadow-[0_-10px_35px_rgba(23,50,77,.08)] backdrop-blur"><div className="grid grid-cols-5 gap-1">{items.map(([href,label,Icon])=>{const active=href==='/'?p===href:p.startsWith(href);return <Link key={href} href={href} className={`rounded-2xl py-2 text-center text-[11px] font-black transition ${active?'bg-accent text-primary':'text-slate-500'}`}><Icon className="mx-auto mb-1 h-5 w-5"/><span>{label}</span></Link>})}</div></nav>}
