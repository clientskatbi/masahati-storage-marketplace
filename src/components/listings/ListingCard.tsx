import Link from "next/link";import { Star,MapPin,Ruler,ShieldCheck } from "lucide-react";import { Listing } from "@/lib/mockData";import { aed } from "@/lib/utils";import { Badge } from "@/components/ui/Badge";
export function ListingCard({listing}:{listing:Listing}){return <article className="overflow-hidden rounded-[1.8rem] border border-slate-200 bg-white shadow-sm"><div className={`h-40 bg-gradient-to-br ${listing.images[0]} relative`}><div className="absolute right-3 top-3 flex gap-2"><Badge>{listing.badge}</Badge>{listing.verified&&<Badge className="bg-emerald-50 text-emerald-700">موثّق</Badge>}</div></div><div className="space-y-3 p-4"><div><h3 className="text-lg font-black text-slate-900">{listing.title}</h3><p className="mt-1 flex items-center gap-1 text-sm text-slate-500"><MapPin className="h-4 w-4"/>{listing.city}، {listing.area}</p></div><div className="grid grid-cols-3 gap-2 text-sm"><span className="rounded-2xl bg-slate-50 p-2 font-bold text-blue-700">{aed(listing.price)}<small className="block text-slate-500">شهريًا</small></span><span className="rounded-2xl bg-slate-50 p-2"><Ruler className="inline h-4 w-4"/> {listing.sqm} م²</span><span className="rounded-2xl bg-slate-50 p-2"><Star className="inline h-4 w-4 fill-amber-400 text-amber-400"/> {listing.rating}</span></div><div className="flex flex-wrap gap-2">{listing.features.slice(0,3).map(f=><Badge key={f} className="bg-slate-50 text-slate-600">{f}</Badge>)}</div><Link href={`/listings/${listing.id}`} className="flex min-h-12 items-center justify-center rounded-2xl bg-slate-900 text-sm font-bold text-white"><ShieldCheck className="ml-2 h-4 w-4"/>عرض التفاصيل</Link></div></article>}
