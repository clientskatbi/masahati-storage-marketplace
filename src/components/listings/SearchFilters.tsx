"use client";import { useState } from "react";import { cities, listings, spaceTypes } from "@/lib/mockData";import { ListingCard } from "./ListingCard";
export function SearchFilters(){const [city,setCity]=useState('الكل');const [type,setType]=useState('الكل');const [sort,setSort]=useState('الأقرب');const data=listings.filter(l=>(city==='الكل'||l.city===city)&&(type==='الكل'||l.type===type)).sort((a,b)=>sort==='الأقل سعرًا'?a.price-b.price:sort==='الأعلى تقييمًا'?b.rating-a.rating:sort==='الأكثر أمانًا'?Number(b.verified)-Number(a.verified):0);return <div className="space-y-4"><div className="rounded-[1.7rem] bg-white p-4 shadow-sm"><input className="mb-3 w-full rounded-2xl border border-slate-200 bg-slate-50 p-4 outline-none focus:ring-2 focus:ring-blue-500" placeholder="ابحث حسب المدينة أو نوع التخزين"/><div className="grid grid-cols-2 gap-3"><select className="rounded-2xl border p-3" value={city} onChange={e=>setCity(e.target.value)}><option>الكل</option>{cities.map(c=><option key={c}>{c}</option>)}</select><select className="rounded-2xl border p-3" value={type} onChange={e=>setType(e.target.value)}><option>الكل</option>{spaceTypes.map(t=><option key={t}>{t}</option>)}</select><input className="rounded-2xl border p-3" placeholder="السعر من"/><input className="rounded-2xl border p-3" placeholder="السعر إلى"/><input className="rounded-2xl border p-3" placeholder="المساحة"/><select className="rounded-2xl border p-3"><option>مدة الوصول</option><option>وصول 24/7</option><option>وصول أسبوعي</option></select></div><div className="mt-3 grid grid-cols-3 gap-2 text-xs font-bold text-slate-700"><label><input type="checkbox"/> مكيف</label><label><input type="checkbox"/> داخلي</label><label><input type="checkbox"/> مناسب للمركبات</label></div><select className="mt-3 w-full rounded-2xl border p-3" value={sort} onChange={e=>setSort(e.target.value)}>{['الأقرب','الأقل سعرًا','الأعلى تقييمًا','الأكثر أمانًا'].map(s=><option key={s}>{s}</option>)}</select></div><div className="h-44 rounded-[1.7rem] bg-[radial-gradient(circle_at_30%_30%,#93c5fd,transparent_30%),linear-gradient(135deg,#e0f2fe,#ecfdf5)] p-4 text-blue-900 shadow-inner"><b>خريطة تجريبية</b><p className="mt-2 text-sm">عرض تقريبي للمناطق بدون خرائط مدفوعة.</p><div className="relative mt-5 h-20"><span className="absolute right-8 top-2 h-5 w-5 rounded-full bg-blue-700 ring-4 ring-white"/><span className="absolute left-16 top-8 h-5 w-5 rounded-full bg-emerald-600 ring-4 ring-white"/><span className="absolute right-36 top-12 h-5 w-5 rounded-full bg-slate-800 ring-4 ring-white"/></div></div>{data.length?data.map(l=><ListingCard key={l.id} listing={l}/>):<div className="rounded-3xl bg-white p-8 text-center font-bold text-slate-500">لا توجد نتائج مطابقة حاليًا</div>}</div>}
