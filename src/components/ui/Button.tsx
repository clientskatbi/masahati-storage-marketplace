import Link from "next/link";import { cn } from "@/lib/utils";
type Props={href?:string;children:React.ReactNode;variant?:"primary"|"secondary"|"ghost"|"danger";className?:string;type?:"button"|"submit";onClick?:()=>void;disabled?:boolean};
export function Button({href,children,variant="primary",className,type="button",onClick,disabled}:Props){const cls=cn("inline-flex min-h-12 items-center justify-center rounded-2xl px-5 py-3 text-sm font-black transition active:scale-[.98] disabled:cursor-not-allowed disabled:opacity-50",variant==="primary"&&"bg-primary text-primary-foreground shadow-lg shadow-slate-900/10",variant==="secondary"&&"bg-secondary text-white shadow-lg shadow-emerald-900/10",variant==="ghost"&&"border border-border bg-card text-foreground",variant==="danger"&&"bg-destructive text-white",className);return href&&!disabled?<Link className={cls} href={href}>{children}</Link>:<button disabled={disabled} onClick={onClick} type={type} className={cls}>{children}</button>}
