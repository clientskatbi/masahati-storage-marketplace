"use client";import { useState } from "react";import { useAppStore } from "@/components/layout/AppStore";import { aed } from "@/lib/utils";import { Button } from "@/components/ui/Button";
export function BookingsClient(){const {bookings}=useAppStore();const [tab,setTab]=useState('نشط');const tabs=['نشط','بانتظار الموافقة','سابق'];const data=bookings.filter(b=>b.status===tab);return <><div className="mb-4 grid grid-cols-3 gap-2">{tabs.map(t=><button onClick={()=>setTab(t)} className={`rounded-2xl p-3 text-sm font-black ${tab===t?'bg-primary text-white':'premium-card'}`} key={t}>{t}</button>)}</div>{data.length?<div className="space-y-3">{data.map(b=><div key={b.id} className="premium-card rounded-[1.5rem] p-4"><div className="flex justify-between gap-3"><b>{b.space}</b><span className="font-black text-primary">{aed(b.price)}</span></div><p className="text-sm text-slate-500">{b.date} — <span className={b.status==='نشط'?'status-active rounded-full px-2':b.status.includes('بانتظار')?'status-pending rounded-full px-2':'status-old rounded-full px-2'}>{b.status}</span></p><div className="mt-3 grid grid-cols-2 gap-2"><Button href={`/listings/${b.listingId}`} variant="ghost">التفاصيل</Button><Button href="/messages/m1" variant="secondary">مراسلة المضيف</Button></div></div>)}</div>:<div className="premium-card rounded-3xl p-8 text-center text-slate-500">لا توجد حجوزات في هذه الحالة.</div>}</>}
